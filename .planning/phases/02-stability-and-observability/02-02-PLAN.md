---
phase: 02
plan: 02-02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/middleware/request_id.py
  - app/main.py
  - tests/test_middleware/test_logging_toggle.py
  - .env.example
requirements:
  - NFR-3
  - NFR-5
autonomous: true
---

<objective>
Add configurable request/response body logging disabled by default for security.
</objective>

<tasks>
<task type="auto">
<read_first>
- app/config.py
</read_first>
<action>
Add two new configuration attributes to `app/config.py` in the `Config` class, after the `LOG_LEVEL` line and before `# Request timeout configuration`:

1. `LOG_REQUEST_BODY = os.environ.get("LOG_REQUEST_BODY", "false").lower() in ("true", "1", "yes")` — defaults to `False`
2. `LOG_RESPONSE_BODY = os.environ.get("LOG_RESPONSE_BODY", "false").lower() in ("true", "1", "yes")` — defaults to `False`
</action>
<acceptance_criteria>
- `app/config.py` contains `LOG_REQUEST_BODY` with default `false`
- `app/config.py` contains `LOG_RESPONSE_BODY` with default `false`
- Both use the same `os.environ.get("...", "false").lower() in ("true", "1", "yes")` boolean pattern as existing config values like `FLASK_DEBUG`
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- app/middleware/request_id.py
- app/config.py
</read_first>
<action>
Extend `app/middleware/request_id.py`:

1. Add `import json` at the top of the file.

2. In the `before_request_setup()` function (inside `init_request_id_middleware`), after the existing `logger.info("Request started ...")` line, add:

   ```python
   if current_app.config.get("LOG_REQUEST_BODY", False):
       body = request.get_data(as_text=True)
       if body:
           logger.info("Request body | id=%s | body=%s", request_id, body)
   ```

   This logs the request body only when the `LOG_REQUEST_BODY` config flag is set to True.

3. In the `after_request_teardown()` function, after the existing `logger.info("Request completed ...")` line, add:

   ```python
   if current_app.config.get("LOG_RESPONSE_BODY", False):
       body = response.get_data(as_text=True)
       if body:
           logger.info("Response body | id=%s | status=%s | body=%s", request_id, response.status_code, body)
   ```

   This logs the response body only when the `LOG_RESPONSE_BODY` config flag is set to True.

4. Add `from flask import current_app` to the existing Flask imports at the top of the file (the import line already includes `from flask import Flask, g, request` — change it to `from flask import Flask, current_app, g, request`).
</action>
<acceptance_criteria>
- `app/middleware/request_id.py` imports `current_app` from flask
- `app/middleware/request_id.py` imports `json`
- `app/middleware/request_id.py` contains `LOG_REQUEST_BODY` string
- `app/middleware/request_id.py` contains `LOG_RESPONSE_BODY` string
- Body logging code is guarded by `if current_app.config.get(...)` checks
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- app/main.py
- app/config.py
</read_first>
<action>
Wire the new config values into `create_app()` in `app/main.py`. After the existing `app.config["RATE_LIMIT_CHAT"] = config.RATE_LIMIT_CHAT` line, add:

```python
app.config["LOG_REQUEST_BODY"] = config.LOG_REQUEST_BODY
app.config["LOG_RESPONSE_BODY"] = config.LOG_RESPONSE_BODY
```
</action>
<acceptance_criteria>
- `app/main.py` contains `app.config["LOG_REQUEST_BODY"] = config.LOG_REQUEST_BODY`
- `app/main.py` contains `app.config["LOG_RESPONSE_BODY"] = config.LOG_RESPONSE_BODY`
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- .env.example
</read_first>
<action>
Append commented environment variables `LOG_REQUEST_BODY` and `LOG_RESPONSE_BODY` (both defaulting to `false`) to `.env.example` with a security warning: "WARNING: Enabling body logging will log the full request and response payloads, which may include sensitive data such as API keys and user messages. Only enable in controlled environments for debugging purposes."
</action>
<acceptance_criteria>
- `.env.example` contains `LOG_REQUEST_BODY` and `LOG_RESPONSE_BODY`
- `.env.example` contains a security warning comment about body logging
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- tests/conftest.py
- tests/test_routes/test_chat.py
- app/middleware/request_id.py
</read_first>
<action>
Create `tests/test_middleware/test_logging_toggle.py`:

1. Create directory `tests/test_middleware/` with an `__init__.py` file.

2. In `test_logging_toggle.py`:
   - Import `pytest` and `from flask import Flask`.
   - Create a pytest fixture `app_with_body_logging` that creates an app with `LOG_REQUEST_BODY=True` and `LOG_RESPONSE_BODY=True` using the same `TestConfig` pattern from `conftest.py` but overriding those two flags.
   - Create a fixture `client_with_logging` from that app.
   - Create class `TestRequestBodyLoggingEnabled`:
     - `test_request_body_logged_when_enabled(client_with_logging, caplog)`: Send a POST to `/v1/chat/completions` with JSON payload `{"model": "test", "messages": []}` (the mock_openwebui_client autouse fixture will handle the backend call). Use `caplog` from pytest to capture log records. Assert that at least one log record contains `"Request body"` and the sent JSON content.
   - Create class `TestRequestBodyLoggingDisabled`:
     - `test_request_body_not_logged_when_disabled(client, caplog)`: Send the same POST to the default app (where body logging is False). Assert that no log record contains `"Request body"`.
   - Create class `TestResponseBodyLoggingEnabled`:
     - `test_response_body_logged_when_enabled(client_with_logging, caplog)`: Send a GET to `/health` (which returns JSON). Assert that at least one log record contains `"Response body"` and `"healthy"`.
   - Create class `TestResponseBodyLoggingDisabled`:
     - `test_response_body_not_logged_when_disabled(client, caplog)`: Send a GET to `/health`. Assert that no log record contains `"Response body"`.
</action>
<acceptance_criteria>
- `tests/test_middleware/__init__.py` exists (empty file)
- `tests/test_middleware/test_logging_toggle.py` exists
- `pytest tests/test_middleware/test_logging_toggle.py -v` exits with code 0
- All 4 test methods pass
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `pytest tests/test_middleware/test_logging_toggle.py -v` -- all tests pass
2. `pytest --cov=app --cov-report=term` -- overall coverage >= 85%
3. Code review confirms body logging is disabled by default
4. Code review confirms security warning exists in `.env.example`
</verification>

<success_criteria>
- `LOG_REQUEST_BODY=false` and `LOG_RESPONSE_BODY=false` by default
- When both flags are `false`, no request/response bodies appear in logs
- When a flag is set to `true`, the corresponding body is logged
- Security warning documented in `.env.example`
- Overall test coverage remains >= 85%
</success_criteria>

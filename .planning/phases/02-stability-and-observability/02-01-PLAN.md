---
phase: 02
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/utils/error_handler.py
  - tests/test_services/test_error_handler.py
requirements:
  - NFR-2
  - NFR-5
autonomous: true
---

<objective>
Expand error handler to cover ConnectionError and SSLError, and fix bare except in status code extraction.
</objective>

<tasks>
<task type="auto">
<read_first>
- app/utils/error_handler.py
</read_first>
<action>
Modify `handle_proxy_error()` in `app/utils/error_handler.py`:

1. Add `from requests.exceptions import ConnectionError as RequestsConnectionError, SSLError` to the import line (rename `ConnectionError` as `RequestsConnectionError` to avoid shadowing the builtin).

2. Insert an `if isinstance(e, RequestsConnectionError):` block BEFORE the existing `if isinstance(e, Timeout):` block. The new block must log a warning with the request_id and return `jsonify({"error": "Backend unreachable", "request_id": request_id})`, 503.

3. Insert an `elif isinstance(e, SSLError):` block AFTER the `Timeout` block. It must log an error with the request_id and return `jsonify({"error": "SSL verification failed", "request_id": request_id})`, 502.

4. Replace the bare `try/except Exception: pass` block inside `if e.response is not None:` with a safe default: set `status_code = e.response.status_code` directly without a try/except. If `e.response.status_code` could raise an `AttributeError` (e.g. `e.response` is not a full Response), use `getattr(e.response, "status_code", 502)` instead of the try/except.
</action>
<acceptance_criteria>
- `app/utils/error_handler.py` imports RequestsConnectionError and SSLError from requests.exceptions
- `app/utils/error_handler.py` contains `"Backend unreachable"` string
- `app/utils/error_handler.py` contains `"SSL verification failed"` string
- `app/utils/error_handler.py` no longer contains a bare `except Exception: pass` block
- `app/utils/error_handler.py` uses `getattr(e.response, "status_code", 502)` instead of the bare try/except
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- tests/test_services/test_openwebui_client.py
- tests/conftest.py
- app/utils/error_handler.py
</read_first>
<action>
Create `tests/test_services/test_error_handler.py` with the following test class and methods:

1. Import `from unittest.mock import Mock, patch` and `from requests.exceptions import RequestException, Timeout, ConnectionError as RequestsConnectionError, SSLError`.

2. Create class `TestErrorHandler` with:
   - `test_timeout_returns_504`: Mocks a `Timeout` exception. Calls `handle_proxy_error(e)`. Asserts response status code equals 504 and JSON body contains `"error": "Request timed out"`.
   - `test_connection_error_returns_503`: Mocks a `RequestsConnectionError`. Calls `handle_proxy_error(e)`. Asserts status code equals 503 and JSON body contains `"Backend unreachable"`.
   - `test_ssl_error_returns_502`: Mocks an `SSLError`. Calls `handle_proxy_error(e)`. Asserts status code equals 502 and JSON body contains `"SSL verification failed"`.
   - `test_generic_request_exception_returns_502`: Mocks a generic `RequestException` with no response attached. Calls `handle_proxy_error(e)`. Asserts status code equals 502 and JSON body contains `"request failed"`.
   - `test_error_with_response_status_code`: Creates a `RequestException` with a mock `response` object whose `status_code` is 429. Calls `handle_proxy_error(e)`. Asserts the returned status code equals 429.
   - `test_request_id_in_response`: Mocks a `RequestException`. Calls `handle_proxy_error(e, endpoint_name="test")`. Asserts JSON body contains a `"request_id"` key with a string value.

Use the existing Flask test client pattern (`client` fixture from conftest.py) where the error handler naturally fires through route calls, OR test `handle_proxy_error` directly by importing it and calling it without a Flask request context. For the direct-import approach, mock `g` from flask to always have `request_id="test-123"`.
</action>
<acceptance_criteria>
- `tests/test_services/test_error_handler.py` exists
- `pytest tests/test_services/test_error_handler.py -v` exits with code 0
- All 6 test methods pass
</acceptance_criteria>
</task>
<task type="auto">
<read_first>
- app/utils/error_handler.py
</read_first>
<action>
Run `pytest --cov=app/utils/error_handler.py --cov-report=term-missing` to verify the error handler coverage exceeds 95%. If any lines are uncovered, add targeted test cases to `tests/test_services/test_error_handler.py`.
</action>
<acceptance_criteria>
- `pytest --cov=app/utils/error_handler.py --cov-report=term-missing` shows coverage >= 95%
- `pytest --cov=app --cov-report=term` shows overall coverage >= 85%
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `pytest tests/test_services/test_error_handler.py -v` -- all 6 tests pass
2. `pytest --cov=app/utils/error_handler.py --cov-report=term-missing` -- coverage >= 95%
3. `pytest --cov=app --cov-report=term` -- overall coverage >= 85%
4. Manual code review confirms bare `except Exception: pass` is removed
</verification>

<success_criteria>
- ConnectionError returns 503 "Backend unreachable"
- SSLError returns 502 "SSL verification failed"
- Timeout still returns 504 "Request timed out"
- Generic errors still return 502 with endpoint name
- Response status codes from the backend are propagated correctly
- Every response includes a request_id
- Error handler coverage >= 95%
- Overall project test coverage >= 85%
</success_criteria>
